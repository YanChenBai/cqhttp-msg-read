module.exports = {
    type_name: "meta_event_type",
    lifecycle: (msg) => {
        // console.log(`this is a lifecycle`);
    },

    heartbeat: (msg) => {
        // console.log(`this is a lifecycle heartbeat`);
    }
}